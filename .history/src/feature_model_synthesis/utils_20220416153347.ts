import { Constrainte } from "../constraints_discovery/constrainte";
import { Feature } from "./feature";

export class Utils {

    existsExcludeConstraint(constraint: Constrainte[], f1: Feature, f2: Feature): boolean {

        let exist = false;

        for (let index = 0; index < constraint.length; index++) {
            const element = constraint[index];

        }

    }


}