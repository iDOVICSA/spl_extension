export class Feature {

    parent: string;
    featureName: string;

    constructor(parent: string, featureName: string) {
        this.parent = parent;
        this.featureName = featureName;
    }
}